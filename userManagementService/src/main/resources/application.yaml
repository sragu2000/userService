server:
  port:
    8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userService1?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: Password123#@!
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update